<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.StaticImg.Views.Components.LayerItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.StaticImg.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:input="using:VirtualPaper.UIComponent.Input"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <input:ArcCheckBox
            MinWidth="0"
            Width="20"
            Margin="0 0 10 0"
            IsChecked="{x:Bind IsEnable, Mode=TwoWay}"/>

        <StackPanel
            Grid.Column="1"
            Orientation="Horizontal"
            Spacing="10">

            <Border
                Width="60" 
                Height="38" 
                BorderBrush="Gray"
                BorderThickness="0.5">
                <Grid>
                    <Image 
                        Stretch="Fill"
                        Source="{StaticResource DraftPanel_Create_StaticImge_LayerThu}"/>

                    <Image  
                        Stretch="Fill"
                        Source="{x:Bind LayerThum, Mode=OneWay}"/>
                </Grid>
            </Border>

            <TextBlock
                x:Name="layerNameTextBlock"
                Grid.Column="1"
                Margin="0 -2 0 0"
                VerticalAlignment="Center" 
                Text="{x:Bind LayerName, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
