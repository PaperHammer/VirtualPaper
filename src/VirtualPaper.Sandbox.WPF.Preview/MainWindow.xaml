<ui:FluentWindow x:Class="VirtualPaper.Sandbox.WPF.Preview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="WPF Sandbox Preview"
        Width="520"
        Height="580"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="WPF Sandbox Preview" />

        <Grid Grid.Row="1" Margin="24,12,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                FontSize="18"
                FontWeight="SemiBold"
                Text="打开窗口"
                Margin="0,0,0,12" />

            <StackPanel Grid.Row="1">
                <ui:Button Content="PluginUpdateWindow"  Appearance="Primary" Margin="0,0,0,8" Click="OpenPluginUpdateWindow" />
                <ui:Button Content="SplashWindow"        Appearance="Primary" Margin="0,0,0,8" Click="OpenSplashWindow" />
                <ui:Button Content="IdentifyWindow"      Appearance="Primary" Margin="0,0,0,8" Click="OpenIdentifyWindow" />
                <ui:Button Content="DebugLog"            Appearance="Primary" Margin="0,0,0,8" Click="OpenDebugLog" />
            </StackPanel>

            <TextBlock
                Grid.Row="2"
                FontSize="14"
                FontWeight="SemiBold"
                Text="已打开窗口"
                Margin="0,16,0,8"
                VerticalAlignment="Bottom" />

            <ListBox x:Name="WindowList"
                     Grid.Row="3"
                     Height="120"
                     Margin="0,0,0,8"
                     DisplayMemberPath="Title" />

            <ui:Button
                Grid.Row="4"
                Content="关闭选中窗口"
                Appearance="Danger"
                HorizontalAlignment="Right"
                Click="CloseWindow_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
