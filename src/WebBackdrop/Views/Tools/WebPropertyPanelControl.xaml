<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebPropertyPanelControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///WebBackdrop/Themes/WebBackdropColors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Padding="12 10 0 10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0 0 16 0">
            <StackPanel Spacing="10">
                <TextBlock
                    FontSize="14"
                    FontWeight="SemiBold"
                    Text="Project Inspector"/>

                <Expander x:Name="projExpander"
                    Header="Wallpaper" 
                    IsExpanded="True"
                    HorizontalAlignment="Stretch" 
                    HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="8">
                        <StackPanel Spacing="2">
                            <TextBlock FontSize="11" Opacity="0.6" Text="Title"/>
                            <TextBlock Text="{x:Bind _viewModel.ProjectTitleText, Mode=OneWay}" TextWrapping="Wrap"/>
                        </StackPanel>
                        <StackPanel Spacing="2">
                            <TextBlock FontSize="11" Opacity="0.6" Text="Entry"/>
                            <TextBlock Text="{x:Bind _viewModel.ProjectEntryText, Mode=OneWay}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </Expander>

                <Expander x:Name="currentItemExpander"
                    Header="Current" 
                    IsExpanded="True" 
                    HorizontalAlignment="Stretch" 
                    HorizontalContentAlignment="Stretch">
                    <StackPanel Spacing="10">
                        <StackPanel Spacing="2" Visibility="{x:Bind _viewModel.NoItemVisibility, Mode=OneWay}">
                            <TextBlock Opacity="0.65" Text="No item selected."/>
                        </StackPanel>

                        <StackPanel Spacing="8" Visibility="{x:Bind _viewModel.ItemInfoVisibility, Mode=OneWay}">
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Name"/>
                                <TextBlock Text="{x:Bind _viewModel.FileNameText, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="11" Opacity="0.6" Text="{x:Bind _viewModel.TypeLabelText, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind _viewModel.TypeText, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Spacing="2" Visibility="{x:Bind _viewModel.StatusVisibility, Mode=OneWay}">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Status"/>
                                <TextBlock Text="{x:Bind _viewModel.StatusText, Mode=OneWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="2" Visibility="{x:Bind _viewModel.SizeVisibility, Mode=OneWay}">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Size"/>
                                <TextBlock Text="{x:Bind _viewModel.SizeText, Mode=OneWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="2" Visibility="{x:Bind _viewModel.LineCountVisibility, Mode=OneWay}">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Lines"/>
                                <TextBlock Text="{x:Bind _viewModel.LineCountText, Mode=OneWay}"/>
                            </StackPanel>
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Modified"/>
                                <TextBlock Text="{x:Bind _viewModel.ModifiedText, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Spacing="2">
                                <TextBlock FontSize="11" Opacity="0.6" Text="Relative Path"/>
                                <TextBlock Text="{x:Bind _viewModel.RelativePathText, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
