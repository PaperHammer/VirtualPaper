<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VirtualPaper.Sandbox.WinUI.Preview.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VirtualPaper.Sandbox.WinUI.Preview"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUI Sandbox Preview">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="24,12,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Settings -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="主题" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox x:Name="ThemeCombo" Width="100" SelectionChanged="ThemeCombo_SelectionChanged">
                    <ComboBoxItem Content="Dark" IsSelected="True" />
                    <ComboBoxItem Content="Light" />
                </ComboBox>

                <TextBlock Text="语言" VerticalAlignment="Center" Margin="20,0,8,0" />
                <ComboBox x:Name="LangCombo" Width="100" SelectionChanged="LangCombo_SelectionChanged">
                    <ComboBoxItem Content="zh-CN" IsSelected="True" />
                    <ComboBoxItem Content="en-US" />
                </ComboBox>
            </StackPanel>

            <TextBlock
                Grid.Row="1"
                FontSize="18"
                FontWeight="SemiBold"
                Text="打开窗口"
                Margin="0,0,0,12" />

            <StackPanel Grid.Row="2">
                <Button Content="WebEditorWindow" Style="{StaticResource AccentButtonStyle}" Margin="0,0,0,8" Click="OpenWebEditorWindow" />
            </StackPanel>

            <TextBlock
                Grid.Row="3"
                FontSize="14"
                FontWeight="SemiBold"
                Text="已打开窗口"
                Margin="0,16,0,8"
                VerticalAlignment="Bottom" />

            <ListBox x:Name="WindowList"
                     Grid.Row="4"
                     Height="120"
                     Margin="0,0,0,8"
                     DisplayMemberPath="Title" />

            <Button
                Grid.Row="5"
                Content="关闭选中窗口"
                HorizontalAlignment="Right"
                Click="CloseWindow_Click" />
        </Grid>
    </Grid>
</Window>
