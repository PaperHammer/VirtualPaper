<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebStatusBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.WebBackdrop.Views.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:VirtualPaper.UIComponent.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///WebBackdrop/Themes/WebBackdropColors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:VisibilityByValueConverter x:Key="VisibilityConverter"/>
            <Style x:Key="StatusBarButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Foreground" Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                <Setter Property="BackgroundSizing" Value="OuterBorderEdge"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter x:Name="ContentPresenter"
                                Background="{TemplateBinding Background}"
                                BackgroundSizing="{TemplateBinding BackgroundSizing}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Foreground="{TemplateBinding Foreground}"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource WebBackdropStatusBarButtonBackgroundPointerOverBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Background" Value="{ThemeResource WebBackdropStatusBarButtonBackgroundPressedBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenter.Opacity" Value="0.4"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="rootGrid" Height="28" Padding="6 0" Background="{ThemeResource LayerFillColorDefaultBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="RunningStates">
                <VisualState x:Name="Idle"/>
                <VisualState x:Name="Running">
                    <VisualState.Setters>
                        <Setter Target="errorActiveIcon.Foreground"   Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="errorActiveText.Foreground"   Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="errorInactiveIcon.Foreground" Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="errorInactiveText.Foreground" Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="warnActiveIcon.Foreground"    Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="warnActiveText.Foreground"    Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="warnInactiveIcon.Foreground"  Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <Setter Target="warnInactiveText.Foreground"  Value="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Stretch">
            <Button Padding="8 2" Style="{StaticResource StatusBarButtonStyle}" VerticalAlignment="Stretch" Click="Problems_Click">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon x:Name="errorActiveIcon"   Glyph="&#xEA39;" FontSize="14" Foreground="{ThemeResource WebBackdropProblemErrorBrush}"    Visibility="{x:Bind ProblemErrorActiveVisibility, Mode=OneWay}"/>
                    <FontIcon x:Name="errorInactiveIcon" Glyph="&#xEA39;" FontSize="14" Foreground="{ThemeResource WebBackdropProblemInactiveBrush}" Visibility="{x:Bind ProblemErrorInactiveVisibility, Mode=OneWay}"/>
                    <TextBlock x:Name="errorActiveText"   FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="{x:Bind ProblemErrorText, Mode=OneWay}"   Foreground="{ThemeResource WebBackdropProblemErrorBrush}"    Visibility="{x:Bind ProblemErrorActiveVisibility, Mode=OneWay}"/>
                    <TextBlock x:Name="errorInactiveText" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="{x:Bind ProblemErrorText, Mode=OneWay}"   Foreground="{ThemeResource WebBackdropProblemInactiveBrush}" Visibility="{x:Bind ProblemErrorInactiveVisibility, Mode=OneWay}"/>
                    <FontIcon x:Name="warnActiveIcon"    Glyph="&#xE7BA;" FontSize="14" Foreground="{ThemeResource WebBackdropProblemWarningBrush}"  Visibility="{x:Bind ProblemWarningActiveVisibility, Mode=OneWay}"/>
                    <FontIcon x:Name="warnInactiveIcon"  Glyph="&#xE7BA;" FontSize="14" Foreground="{ThemeResource WebBackdropProblemInactiveBrush}" Visibility="{x:Bind ProblemWarningInactiveVisibility, Mode=OneWay}"/>
                    <TextBlock x:Name="warnActiveText"    FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="{x:Bind ProblemWarningText, Mode=OneWay}" Foreground="{ThemeResource WebBackdropProblemWarningBrush}"  Visibility="{x:Bind ProblemWarningActiveVisibility, Mode=OneWay}"/>
                    <TextBlock x:Name="warnInactiveText"  FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="{x:Bind ProblemWarningText, Mode=OneWay}" Foreground="{ThemeResource WebBackdropProblemInactiveBrush}" Visibility="{x:Bind ProblemWarningInactiveVisibility, Mode=OneWay}"/>
                </StackPanel>
            </Button>

            <Button Padding="8 2" Style="{StaticResource StatusBarButtonStyle}" VerticalAlignment="Stretch" Click="Run_Click">
                <Grid>
                    <!-- Run state -->
                    <StackPanel Orientation="Horizontal" Spacing="4"
                            Visibility="{x:Bind IsDebugRunning, Mode=OneWay, Converter={StaticResource VisibilityConverter}, ConverterParameter=!}">
                        <FontIcon Glyph="&#xE768;" FontSize="14" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <TextBlock FontSize="11" Text="运行"/>
                    </StackPanel>
                    <!-- Stop state -->
                    <StackPanel Orientation="Horizontal" Spacing="4"
                            Visibility="{x:Bind IsDebugRunning, Mode=OneWay, Converter={StaticResource VisibilityConverter}}">
                        <FontIcon Glyph="&#xE71A;" FontSize="14" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                        <TextBlock FontSize="11" Text="停止" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                    </StackPanel>
                </Grid>
            </Button>
            <!--<Button Padding="8 2" Style="{StaticResource StatusBarButtonStyle}" ToolTipService.ToolTip="Debug" Click="Debug_Click">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xEBE8;" FontSize="14" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                    <TextBlock FontSize="11" Text="调试"/>
                </StackPanel>
            </Button>-->
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center" Visibility="{x:Bind SavingVisibility, Mode=OneWay}">
                <ProgressRing Width="12" Height="12" IsActive="{x:Bind IsSaving, Mode=OneWay}" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
                <TextBlock FontSize="11" Text="正在保存..." Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
            <Button Padding="5" Style="{StaticResource StatusBarButtonStyle}" Click="ToggleLeftSideBar_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource WebBackdropStatusBarForegroundBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Width="6" HorizontalAlignment="Left" Background="{ThemeResource WebBackdropStatusBarForegroundBrush}" CornerRadius="2 0 0 2" Visibility="{x:Bind IsLeftSideBarVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}}"/>
                    <Rectangle Width="1" HorizontalAlignment="Left" Margin="5,0,0,0" Fill="{ThemeResource WebBackdropStatusBarForegroundBrush}" Visibility="{x:Bind IsLeftSideBarVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}, ConverterParameter=!}"/>
                </Grid>
            </Button>
            <Button Padding="5" Style="{StaticResource StatusBarButtonStyle}" Click="ToggleBottomPanel_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource WebBackdropStatusBarForegroundBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Height="6" VerticalAlignment="Bottom" Background="{ThemeResource WebBackdropStatusBarForegroundBrush}" CornerRadius="0 0 2 2" Visibility="{x:Bind IsBottomPanelVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}}"/>
                    <Rectangle Height="1" VerticalAlignment="Bottom" Margin="0,0,0,5" Fill="{ThemeResource WebBackdropStatusBarForegroundBrush}" Visibility="{x:Bind IsBottomPanelVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}, ConverterParameter=!}"/>
                </Grid>
            </Button>
            <Button Padding="5" Style="{StaticResource StatusBarButtonStyle}" Click="ToggleRightSideBar_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource WebBackdropStatusBarForegroundBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Width="6" HorizontalAlignment="Right" Background="{ThemeResource WebBackdropStatusBarForegroundBrush}" CornerRadius="0 2 2 0" Visibility="{x:Bind IsRightSideBarVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}}"/>
                    <Rectangle Width="1" HorizontalAlignment="Right" Margin="0,0,5,0" Fill="{ThemeResource WebBackdropStatusBarForegroundBrush}" Visibility="{x:Bind IsRightSideBarVisible, Mode=OneWay, Converter={StaticResource VisibilityConverter}, ConverterParameter=!}"/>
                </Grid>
            </Button>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}" VerticalAlignment="Center" Text="{x:Bind CursorPositionText, Mode=OneWay}" Visibility="{x:Bind IsTextFile, Converter={StaticResource VisibilityConverter}}"/>
            <Button Padding="8 0" Style="{StaticResource StatusBarButtonStyle}" VerticalAlignment="Stretch" Visibility="{x:Bind IsTextFile, Converter={StaticResource VisibilityConverter}}" Click="Indent_Click">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Name="indentTab2" Text="Tabs: 2" Click="IndentOption_Click" Tag="2,tabs"/>
                        <MenuFlyoutItem x:Name="indentTab4" Text="Tabs: 4" Click="IndentOption_Click" Tag="4,tabs"/>
                        <MenuFlyoutItem x:Name="indentSpace2" Text="Spaces: 2" Click="IndentOption_Click" Tag="2,spaces"/>
                        <MenuFlyoutItem x:Name="indentSpace4" Text="Spaces: 4" Click="IndentOption_Click" Tag="4,spaces"/>
                        <MenuFlyoutItem x:Name="indentSpace8" Text="Spaces: 8" Click="IndentOption_Click" Tag="8,spaces"/>
                    </MenuFlyout>
                </Button.Flyout>
                <TextBlock FontSize="11" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}" VerticalAlignment="Center" Text="{x:Bind IndentText, Mode=OneWay}"/>
            </Button>
            <Button Padding="8 0" Style="{StaticResource StatusBarButtonStyle}" VerticalAlignment="Stretch" Visibility="{x:Bind IsTextFile, Converter={StaticResource VisibilityConverter}}" Click="Encoding_Click">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="UTF-8" Click="EncodingOption_Click" Tag="UTF-8"/>
                        <MenuFlyoutItem Text="UTF-8 BOM" Click="EncodingOption_Click" Tag="UTF-8 BOM"/>
                        <MenuFlyoutItem Text="UTF-16 LE" Click="EncodingOption_Click" Tag="UTF-16 LE"/>
                        <MenuFlyoutItem Text="UTF-16 BE" Click="EncodingOption_Click" Tag="UTF-16 BE"/>
                    </MenuFlyout>
                </Button.Flyout>
                <TextBlock FontSize="11" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}" VerticalAlignment="Center" Text="{x:Bind EncodingText, Mode=OneWay}"/>
            </Button>
            <Button Padding="8 0" Style="{StaticResource StatusBarButtonStyle}" VerticalAlignment="Stretch" Visibility="{x:Bind IsTextFile, Converter={StaticResource VisibilityConverter}}" Click="LineEnding_Click">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="LF" Click="LineEndingOption_Click" Tag="LF"/>
                        <MenuFlyoutItem Text="CRLF" Click="LineEndingOption_Click" Tag="CRLF"/>
                    </MenuFlyout>
                </Button.Flyout>
                <TextBlock FontSize="11" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}" VerticalAlignment="Center" Text="{x:Bind LineEndingText, Mode=OneWay}"/>
            </Button>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource WebBackdropStatusBarForegroundBrush}" VerticalAlignment="Center" Text="{x:Bind LanguageStatusText, Mode=OneWay}" Visibility="{x:Bind IsTextFile, Converter={StaticResource VisibilityConverter}}"/>
        </StackPanel>
    </Grid>

</UserControl>
