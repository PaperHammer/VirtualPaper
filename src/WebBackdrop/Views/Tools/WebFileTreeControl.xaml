<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebFileTreeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.WebBackdrop.Views.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Padding="8 6" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                VerticalAlignment="Center"
                FontSize="12"
                FontWeight="SemiBold"
                Text="{x:Bind ProjectName, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"/>
            <Button
                Grid.Column="1"
                Padding="4"
                BorderThickness="0"
                Background="Transparent"
                ToolTipService.ToolTip="New File"
                Click="NewFile_Click">
                <FontIcon Glyph="&#xE8A5;" FontSize="14"/>
            </Button>
        </Grid>

        <!-- File list -->
        <ListView
            Grid.Row="1"
            x:Name="fileListView"
            SelectionMode="Single"
            ItemsSource="{x:Bind FileItems, Mode=OneWay}"
            SelectionChanged="FileListView_SelectionChanged">
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                    <Setter Property="Padding" Value="8 4"/>
                    <Setter Property="MinHeight" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:WebFileItem">
                    <Grid ColumnSpacing="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="{x:Bind FileGlyph}" FontSize="12" VerticalAlignment="Center"/>
                        <TextBlock
                            Grid.Column="1"
                            FontSize="12"
                            VerticalAlignment="Center"
                            Text="{x:Bind FileName}"
                            TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
