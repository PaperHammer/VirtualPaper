<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebStatusBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.WebBackdrop.Views.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Height="28" Padding="6 0" Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
            <Button Padding="8 2" BorderThickness="0" Background="Transparent" Foreground="{ThemeResource TextFillColorPrimaryBrush}" Click="Problems_Click">
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <FontIcon Glyph="&#xEA39;" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="0"/>
                    <FontIcon Glyph="&#xE7BA;" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" LineHeight="13" LineStackingStrategy="BlockLineHeight" Text="0"/>
                </StackPanel>
            </Button>

            <Button Padding="8 2" BorderThickness="0" CornerRadius="4" Background="Transparent" Foreground="{ThemeResource TextFillColorPrimaryBrush}" ToolTipService.ToolTip="Run" Click="Run_Click">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xE768;" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock FontSize="11" Text="运行"/>
                </StackPanel>
            </Button>
            <Button Padding="8 2" BorderThickness="0" CornerRadius="4" Background="Transparent" Foreground="{ThemeResource TextFillColorPrimaryBrush}" ToolTipService.ToolTip="Debug" Click="Debug_Click">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xEBE8;" FontSize="14" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <TextBlock FontSize="11" Text="调试"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
            <Button Padding="5" BorderThickness="0" Background="Transparent" ToolTipService.ToolTip="切换左侧栏" Click="ToggleLeftSideBar_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Width="6" HorizontalAlignment="Left" Background="{ThemeResource TextFillColorPrimaryBrush}" CornerRadius="2 0 0 2" Visibility="{x:Bind LeftSideBarFillVisibility, Mode=OneWay}"/>
                    <Rectangle Width="1" HorizontalAlignment="Left" Margin="5,0,0,0" Fill="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind LeftSideBarLineVisibility, Mode=OneWay}"/>
                </Grid>
            </Button>
            <Button Padding="5" BorderThickness="0" Background="Transparent" ToolTipService.ToolTip="切换底部面板" Click="ToggleBottomPanel_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Height="6" VerticalAlignment="Bottom" Background="{ThemeResource TextFillColorPrimaryBrush}" CornerRadius="0 0 2 2" Visibility="{x:Bind BottomPanelFillVisibility, Mode=OneWay}"/>
                    <Rectangle Height="1" VerticalAlignment="Bottom" Margin="0,0,0,5" Fill="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind BottomPanelLineVisibility, Mode=OneWay}"/>
                </Grid>
            </Button>
            <Button Padding="5" BorderThickness="0" Background="Transparent" ToolTipService.ToolTip="切换右侧栏" Click="ToggleRightSideBar_Click">
                <Grid Width="14" Height="14">
                    <Border BorderBrush="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="1" CornerRadius="2"/>
                    <Border Width="6" HorizontalAlignment="Right" Background="{ThemeResource TextFillColorPrimaryBrush}" CornerRadius="0 2 2 0" Visibility="{x:Bind RightSideBarFillVisibility, Mode=OneWay}"/>
                    <Rectangle Width="1" HorizontalAlignment="Right" Margin="0,0,5,0" Fill="{ThemeResource TextFillColorPrimaryBrush}" Visibility="{x:Bind RightSideBarLineVisibility, Mode=OneWay}"/>
                </Grid>
            </Button>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind LineText, Mode=OneWay}"/>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind ColumnText, Mode=OneWay}"/>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind IndentText, Mode=OneWay}"/>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind EncodingText, Mode=OneWay}"/>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind LineEndingText, Mode=OneWay}"/>
            <TextBlock Padding="8 0" FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" Text="{x:Bind LanguageStatusText, Mode=OneWay}"/>
        </StackPanel>
    </Grid>

</UserControl>
