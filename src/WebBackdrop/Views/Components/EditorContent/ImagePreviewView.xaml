<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Components.EditorContent.ImagePreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        PointerWheelChanged="ImagePreviewPanel_PointerWheelChanged">
        <ScrollViewer x:Name="imageScrollViewer"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            ZoomMode="Disabled"
            SizeChanged="ImageScrollViewer_SizeChanged">
            <Image x:Name="imagePreview"
                Margin="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Stretch="Uniform"
                ImageOpened="ImagePreview_ImageOpened"
                ImageFailed="ImagePreview_ImageFailed"/>
        </ScrollViewer>

        <Grid x:Name="previewOverlay"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            IsHitTestVisible="False"/>
    </Grid>
</UserControl>
