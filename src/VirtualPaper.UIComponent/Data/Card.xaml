<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VirtualPaper.UIComponent.Data.Card"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VirtualPaper.UIComponent.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border x:Name="PART_CardBorder"
        CornerRadius="{x:Bind CornerRadius, Mode=OneWay}"
        Padding="{x:Bind Padding, Mode=OneWay}"
        Background="{x:Bind Background, Mode=OneWay}"
        BorderBrush="{x:Bind BorderBrush, Mode=OneWay}"
        BorderThickness="{x:Bind BorderThickness, Mode=OneWay}"
        HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch"
        Translation="{x:Bind ElevationVector, Mode=OneWay}">
        <Border.Shadow>
            <ThemeShadow />
        </Border.Shadow>

        <ContentPresenter x:Name="PART_RootContent" Content="{x:Bind RootContent, Mode=OneWay}" />
    </Border>
</UserControl>
