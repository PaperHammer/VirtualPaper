<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Workloads.Creation.StaticImg.Views.Components.Export"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.StaticImg.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:markup="using:VirtualPaper.UIComponent.ArcMarkupExtension"
    xmlns:workloads_models="using:Workloads.Utils.DraftUtils.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid RowDefinitions="*, Auto" RowSpacing="16">
            <Border 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="16">
                <Image 
                    Source="{x:Bind _viewModel.PreviewImageSource, Mode=OneWay}" 
                    Stretch="Uniform" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"/>
            </Border>

            <StackPanel 
                Grid.Row="1" 
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16" Spacing="8">
                
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
            <StackPanel Spacing="20" VerticalAlignment="Center">

                <TextBlock Text="导出设置" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8" />

                <TextBox x:Name="tbSearchName"
                    Header="文件名称" 
                    Text="{x:Bind _viewModel.ExportName, Mode=TwoWay}"
                    Height="30"
                    Width="340"
                    PlaceholderText="{markup:I18n Key=Project_SearchTemplate}"
                    FontSize="12"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"/>

                <Grid ColumnDefinitions="*, Auto" ColumnSpacing="8">
                    <TextBox Grid.Column="0" 
                             Header="保存位置" 
                             IsReadOnly="True"
                             Text="{x:Bind _viewModel.ExportPath}" />
                    <Button Grid.Column="1" 
                            Content="浏览..." 
                            VerticalAlignment="Bottom" 
                            Command="{x:Bind _viewModel.BrowsePathCommand}" />
                </Grid>

                <ComboBox Header="图片格式" 
                          HorizontalAlignment="Stretch" 
                          ItemsSource="{x:Bind _viewModel.AvailableFormats}" 
                          SelectedItem="{x:Bind _viewModel.SelectedFormat, Mode=TwoWay}" />

                <StackPanel Spacing="4">
                    <TextBlock Text="缩放比例" />
                    <ItemsControl ItemsSource="{x:Bind _viewModel.ScaleOptions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="16"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="workloads_models:ScaleOption">
                                <CheckBox Content="{x:Bind DisplayName}" 
                                          IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <StackPanel Visibility="{x:Bind _viewModel.IsJpegQualityVisible}">
                    <TextBlock Text="JPEG 质量" Margin="0,0,0,4"/>
                    <Grid ColumnDefinitions="*, Auto" ColumnSpacing="16">
                        <Slider Grid.Column="0" Minimum="1" Maximum="100" 
                                Value="{x:Bind _viewModel.JpegQuality, Mode=TwoWay}" 
                                VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                   Text="{x:Bind _viewModel.JpegQuality, Mode=OneWay}" 
                                   Width="30" TextAlignment="Right"/>
                    </Grid>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
