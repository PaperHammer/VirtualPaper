<Window x:Class="VirtualPaper.Views.ScreenSaverPreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VirtualPaper.Views"
        mc:Ignorable="d"
        Title="Screensaver Preview"     
        Width="156"
        Height="108">

    <Window.Resources>
        <Storyboard x:Key="FadeInOutStoryboard">
            <DoubleAnimation
            Storyboard.TargetName="vpIcon"
            Storyboard.TargetProperty="Opacity"
            From="0.0" To="1.0"
            Duration="0:0:1"
            AutoReverse="True"
            RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Image
             x:Name="vpIcon"
            
             Source="pack://application:,,,/Resources/appicon_180.png">
            <Image.Triggers>
                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                    <BeginStoryboard Storyboard="{StaticResource FadeInOutStoryboard}"/>
                </EventTrigger>
            </Image.Triggers>
        </Image>
    </Grid>

</Window>
