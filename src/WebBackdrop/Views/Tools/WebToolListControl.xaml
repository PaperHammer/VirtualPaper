<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebToolListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.WebBackdrop.Views.Tools"
    xmlns:models="using:Workloads.Creation.WebBackdrop.Models"
    xmlns:collection="using:VirtualPaper.UIComponent.Collection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <UserControl.Resources>
        <DataTemplate x:Key="WebToolItemTemplate" x:DataType="models:WebToolItem">
            <Grid>
                <FontIcon
                    Visibility="{x:Bind Glyph, Converter={StaticResource VisibilityByValueConverter}, ConverterParameter=HasValue}"
                    Glyph="{x:Bind Glyph}"
                    Width="20"/>
                <ToolTipService.ToolTip>
                    <ToolTip Placement="Mouse" Content="{x:Bind ToolName}" Visibility="Visible"/>
                </ToolTipService.ToolTip>
            </Grid>
        </DataTemplate>

        <Style x:Key="WebToolItemStyle" BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
    </UserControl.Resources>

    <collection:ArcListView
        x:Name="arcListView"
        Width="40"
        Loaded="ArcListView_Loaded"
        CancelSelectionEnable="False"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        ScrollViewer.VerticalScrollBarVisibility="Hidden"
        ScrollViewer.HorizontalScrollMode="Disabled"
        ScrollViewer.VerticalScrollMode="Auto"
        SelectedItem="{Binding SelectedTool, Mode=TwoWay}"
        ItemsSource="{x:Bind ToolItems, Mode=OneWay}"
        ItemTemplate="{StaticResource WebToolItemTemplate}"
        ItemContainerStyle="{StaticResource WebToolItemStyle}"/>
</UserControl>
