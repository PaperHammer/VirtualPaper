<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.StaticImg.Views.Components.InkCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.StaticImg.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">
    
    <Grid
        x:Name="container"
        Background="Transparent"
        BorderThickness="1"
        BorderBrush="Gray"
        DataContext="{x:Bind _viewModel.BasicData, Mode=OneWay}"
        Width="{x:Bind _viewModel.BasicData.Size.Width, Mode=OneWay}"
        Height="{x:Bind _viewModel.BasicData.Size.Height, Mode=OneWay}">
        <Grid
            x:Name="workground"
            Canvas.ZIndex="-1"/>
        <win2d:CanvasControl
            x:Name="inkCanvas"
            Loaded="InkingCanvas_Loaded"
            Draw="InkCanvas_Draw"/>
    </Grid>
</UserControl>
