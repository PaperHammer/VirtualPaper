<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Workloads.Creation.WebBackdrop.Views.Tools.WebProjectInfoControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workloads.Creation.WebBackdrop.Views.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="12 8" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12">
            <!-- Title -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="11" Opacity="0.6" Text="Title"/>
                <TextBox x:Name="TitleBox" PlaceholderText="Wallpaper title" TextChanged="TitleBox_TextChanged"/>
            </StackPanel>

            <!-- Description -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="11" Opacity="0.6" Text="Description"/>
                <TextBox
                    x:Name="DescBox"
                    PlaceholderText="Short description"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    Height="72"
                    TextChanged="DescBox_TextChanged"/>
            </StackPanel>

            <!-- Author -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="11" Opacity="0.6" Text="Author"/>
                <TextBox x:Name="AuthorBox" PlaceholderText="Author name" TextChanged="AuthorBox_TextChanged"/>
            </StackPanel>

            <!-- Tags -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="11" Opacity="0.6" Text="Tags (semicolon separated)"/>
                <TextBox x:Name="TagsBox" PlaceholderText="tag1; tag2" TextChanged="TagsBox_TextChanged"/>
            </StackPanel>

            <!-- Entry file -->
            <StackPanel Spacing="4">
                <TextBlock FontSize="11" Opacity="0.6" Text="Entry file"/>
                <TextBox x:Name="EntryFileBox" PlaceholderText="index.html" TextChanged="EntryFileBox_TextChanged"/>
            </StackPanel>

            <!-- Save button -->
            <Button
                HorizontalAlignment="Stretch"
                Content="Save project.json"
                Click="SaveProjectInfo_Click"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
