<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VirtualPaper.UIComponent.Data.CheckBoxItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VirtualPaper.UIComponent.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:input="using:VirtualPaper.UIComponent.Input"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <input:ArcCheckBox
            MinWidth="0"
            Width="20"
            Margin="0 0 10 0"
            IsChecked="{x:Bind IsEnable, Mode=TwoWay}"/>
        
        <StackPanel
            Grid.Column="1"
            Orientation="Horizontal"
            Spacing="4">

            <!--<Image
                Width="55"
                Source="{x:Bind LayerThum, Mode=OneWay}"/>-->
            <ContentControl x:Name="layerThum" Width="62" Height="40"/>

            <TextBlock
                Grid.Column="1"
                Margin="0 -2 0 0"
                VerticalAlignment="Center" 
                Text="{x:Bind LayerName, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
