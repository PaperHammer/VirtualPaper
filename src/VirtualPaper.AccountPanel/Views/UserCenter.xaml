<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VirtualPaper.AccountPanel.Views.UserCenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VirtualPaper.AccountPanel.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cvs="using:VirtualPaper.UIComponent.Converters"
    mc:Ignorable="d"    
    NavigationCacheMode="Enabled">

    <Page.Resources>
        <cvs:ByteToImageConverter x:Key="ByteToImageConverter"/>
    </Page.Resources>

    <Grid x:Name="gridUserCenter"
          Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid 
            Background="{ThemeResource SolidBackgroundFillColorBaseAltBrush}"
            Margin="10"
            Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Button 
                x:Name="btnEdit" 
                CornerRadius="100"
                Height="100" 
                Width="100" 
                Margin="10"
                Padding="0" 
                Click="EditUserInfoButton_Click">
                <Image 
                    Height="100" 
                    Width="100"
                    Stretch="UniformToFill" 
                    Source="{x:Bind _viewModel.User.Avatar, Mode=OneWay, Converter={StaticResource ByteToImageConverter}}"/>
            </Button>

            <Grid 
                Grid.Column="1"
                VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.3*"/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock 
                    x:Name="txbUserName" 
                    Text="{x:Bind _viewModel.User.Name, Mode=OneWay}" 
                    FontSize="24"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left" 
                    TextWrapping="Wrap" 
                    FontWeight="Bold" 
                    Margin="0 4"/>
                <TextBlock 
                    x:Name="txbUserSign" 
                    Grid.Row="1" 
                    Text="{x:Bind _viewModel.Sign, Mode=OneWay}" 
                    FontSize="14" 
                    Margin="0 4"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left"/>
                <TextBlock 
                    x:Name="txbUid" 
                    Grid.Row="2" 
                    Text="{x:Bind _viewModel.User.Uid, Mode=OneWay}" 
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    FontSize="14" 
                    Width="auto" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left"
                    Margin="0 4"/>
            </Grid>

            <Button 
                x:Name="btnLogout" 
                Grid.Column="2"
                Content="{x:Bind _viewModel.Account_Logout}"
                Click="LogoutButton_Click"/>
        </Grid>

        <Grid
            x:Name="gridUserCenterData"
            Margin="10 0"
            Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <SelectorBar 
                x:Name="SelBarAccopunt"
                SelectionChanged="SelectorBar_SelectionChanged"
                Background="{ThemeResource SolidBackgroundFillColorSecondaryBrush}"
                HorizontalAlignment="Stretch">
                <SelectorBarItem x:Name="s1" Text="{x:Bind _selBarItem1}" Tag="CloudView" IsSelected="True"/>
                <!--<SelectorBarItem x:Name="s2" Text="{x:Bind _selBarItem2}" Tag="StarView"/>-->
                <SelectorBarItem x:Name="s3" Text="{x:Bind _selBarItem3}" Tag="UplaodView"/>
            </SelectorBar>

            <Frame 
                x:Name="ContentFrame" 
                Grid.Row="1"/>
        </Grid>
    </Grid>
</Page>
